{
  "name": "neurorehab-dose-tracker",
  "version": "6.0.0",
  "private": true,
  "description": "Offline-first multilingual neurorehabilitation dose and equipment tracking prototype",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check i18n.js && node --check app-v4.js && node --check equipment-upgrade.js"
  },
  "devDependencies": {
    "jsdom": "^27.0.0"
  },
  "license": "MIT"
}
